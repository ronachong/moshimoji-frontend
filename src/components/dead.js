// -------------------------------------
// // implemented a dropdown button but realized a ready made solution that did
// // more (take in input) was available in the form of react-select
//
// import { MenuItem, DropdownButton } from 'react-bootstrap';
//
// const MenuItems = ({ items }) => (
//   items.map(
//     (item, i) => (
//       <MenuItem eventKey={i} key={i}>{item}</MenuItem>
//     ),
//   )
// );
//
// <DropdownButton
//   bsStyle="default"
//   title="test title"
//   key={1}
//   id="dropdown-basic-1" >
//   <MenuItems items={series} />
// </DropdownButton>
//
